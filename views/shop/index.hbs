{{# each products}}
<div class="row">
  {{# each this}}
  <div class="col-sm-6 col-md-4">
    <div class="thumbnail">
      <img src="{{this.imagePath}}" alt="..." class="img-responsive">
      <div class="caption">
        <h3>{{this.title}}</h3><h4 align="right" color="#D5DBDB">{{this.entreprise}}</h4>
        <p class="description">{{this.description}}</p>
        <div class="clearfix">
          <div>{{this.price}} €</div>
          <a href="/panier/ajouter-au-panier/{{this.id}}" class="btn btn-success pull-right" role="button">Ajouter au panier</a>
        </div>
      </div>
    </div>
  </div>
  {{/each}}
</div>
{{/each}}

</script>
